# 🚀 XUI Panel Manager - Complete Edition

**سیستم مدیریت جامع و حرفه‌ای برای پنل‌های X-UI/3X-UI**

یک راهکار All-in-One برای مدیریت چندین پنل X-UI با امکانات پیشرفته:

## ✨ ویژگی‌های اصلی

### 🌐 پنل مدیریت وب
- **داشبورد کامل**: نمایش آمار و وضعیت real-time
- **مدیریت کلاینت‌ها**: ویرایش مستقیم ترافیک و زمان
- **سیستم پکیج**: تعریف و اعمال پکیج‌های آماده با یک کلیک
- **مدیریت سرورها**: اضافه/حذف سرور در حین کار
- **چند زبانه**: پشتیبانی کامل از فارسی و انگلیسی

### 🔄 همگام‌سازی هوشمند
- **مقایسه همه سرورها**: جمع‌آوری و تحلیل داده از همه پنل‌ها
- **ماکزیمم ترافیک**: انتخاب بیشترین مصرف (up/down)
- **مینیمم زمان**: کمترین روز باقی‌مانده در تمام سرورها
- **اعمال یکپارچه**: به‌روزرسانی همزمان همه سرورها

### 📦 سیستم پکیج پیشرفته
- **پکیج‌های قابل تنظیم**: تعریف نامحدود پکیج با قیمت
- **اعمال سریع**: یک کلیک برای Apply کردن پکیج به کلاینت
- **ریست ترافیک**: صفر کردن ترافیک مصرفی
- **افزودن/تنظیم**: امکان اضافه یا جایگزینی مقادیر

### 🔔 اعلان‌های تلگرام
- **نوتیفیکیشن خودکار**: اطلاع از sync موفق/ناموفق
- **هشدار تغییرات**: اعلام اعمال پکیج یا ویرایش کلاینت
- **پیام سفارشی**: قابلیت ارسال پیام‌های مدیریتی

### 💾 پشتیبان‌گیری خودکار
- **Backup قبل از Sync**: ذخیره خودکار قبل از هر تغییر
- **نگهداری چندین نسخه**: حفظ 10 backup اخیر
- **Rollback آسان**: بازگردانی در صورت مشکل

### 🔍 نظارت و گزارش‌دهی
- **Health Check**: بررسی وضعیت سرورها
- **لاگ‌های جامع**: ثبت تمام عملیات و خطاها
- **آمار تفصیلی**: نمودار مصرف و وضعیت کلاینت‌ها

### 🔒 امنیت
- **SSH Key-Based**: احراز هویت امن با کلید
- **احراز هویت وب**: سیستم لاگین برای پنل
- **لاگ تغییرات**: ثبت کامل عملیات کاربران

---

## 📥 نصب

### پیش‌نیازها
- Ubuntu 20.04 یا بالاتر
- دسترسی root
- پنل X-UI یا 3X-UI نصب شده

### نصب سریع (یک خطی)

```bash
wget -O install.sh https://raw.githubusercontent.com/YOUR_REPO/xui-panel-manager-installer.sh && \
chmod +x install.sh && \
sudo ./install.sh
```

### نصب دستی

```bash
# 1. دانلود installer
wget https://raw.githubusercontent.com/YOUR_REPO/xui-panel-manager-installer.sh

# 2. اجازه اجرا
chmod +x xui-panel-manager-installer.sh

# 3. اجرای نصب
sudo ./xui-panel-manager-installer.sh
```

### مراحل نصب

1. **انتخاب زبان**
   ```
   Select Language / انتخاب زبان
   1) English
   2) فارسی
   ```

2. **تنظیمات اولیه**
   - بازه همگام‌سازی (پیش‌فرض: 60 دقیقه)
   - پورت پنل وب (پیش‌فرض: 8080)
   - راه‌اندازی تلگرام (اختیاری)

3. **کپی کلید SSH**
   
   پس از نصب، کلید عمومی نمایش داده می‌شود:
   ```bash
   ssh-ed25519 AAAA... xui-panel-manager
   ```
   
   این کلید را روی **همه سرورهای دیگر** کپی کنید:
   ```bash
   # روی هر سرور:
   echo "ssh-ed25519 AAAA... xui-panel-manager" >> ~/.ssh/authorized_keys
   ```
   
   یا استفاده از ssh-copy-id:
   ```bash
   ssh-copy-id -i /etc/xui-panel-manager/id_ed25519.pub root@SERVER_IP
   ```

4. **دسترسی به پنل**
   
   بعد از نصب، به آدرس زیر بروید:
   ```
   http://YOUR_SERVER_IP:8080
   ```
   
   نام کاربری و رمز پیش‌فرض:
   ```
   Username: admin
   Password: admin123
   ```
   
   ⚠️ **حتماً بعد از اولین ورود رمز را تغییر دهید!**

---

## 🎯 راهنمای استفاده

### 1️⃣ اضافه کردن سرورها

#### از طریق Terminal:
```bash
sudo xui-panel-manager
# انتخاب گزینه: "Add server"
```

#### از طریق Web Panel:
1. ورود به پنل
2. منوی **Servers** → **Add Server**
3. وارد کردن اطلاعات:
   - نام سرور (مثلاً: Server-Germany)
   - آدرس IP
   - پورت SSH
   - نام کاربری
   - مسیر دیتابیس
   - نام سرویس

### 2️⃣ تعریف پکیج‌ها

1. منوی **Packages** → **Add Package**
2. تعریف پکیج:
   ```
   نام: Basic 1 Month
   روز: 30
   حجم: 50 GB
   قیمت: $10 (اختیاری)
   ```

**پکیج‌های پیشنهادی:**

| نام | روز | حجم | قیمت |
|-----|-----|-----|------|
| Basic | 30 | 50 GB | $10 |
| Standard | 30 | 100 GB | $15 |
| Premium | 30 | 200 GB | $25 |
| Unlimited | 30 | 1000 GB | $50 |

### 3️⃣ مدیریت کلاینت‌ها

#### اعمال پکیج:
1. منوی **Clients**
2. جستجوی کلاینت
3. کلیک روی **📦 Package**
4. انتخاب پکیج
5. **Apply**

✅ **نتیجه**: ترافیک reset + زمان جدید + sync به همه سرورها

#### ویرایش دستی:
1. کلیک روی **✏️ Edit**
2. تنظیم:
   - **Days**: تعداد روز
   - **Traffic**: حجم (GB)
   - **Action**:
     - `Set`: جایگزینی مقادیر
     - `Add`: اضافه به موجودی
     - `Reset`: فقط reset traffic

#### صفر کردن ترافیک:
کلیک روی **🔄 Reset** → تأیید

### 4️⃣ راه‌اندازی تلگرام

#### 1. ساخت ربات:
1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot`
3. نام و username ربات را وارد کنید
4. **Bot Token** را ذخیره کنید

#### 2. دریافت Chat ID:
1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. عدد **ID** را یادداشت کنید

#### 3. تنظیم در پنل:
```bash
# از Terminal:
sudo xui-panel-manager
# گزینه: "Telegram bot settings"

# یا از Web Panel:
Settings → Telegram
```

وارد کردن:
```
Bot Token: 123456:ABC-DEF...
Chat ID: 123456789
```

✅ بعد از تنظیم، اعلان‌های زیر ارسال می‌شود:
- ✅ Sync موفق
- ❌ خطا در Sync
- 📦 اعمال پکیج
- ✏️ ویرایش کلاینت
- 🔄 Reset ترافیک

---

## 🔧 مدیریت از Terminal

```bash
# دسترسی به منوی اصلی
sudo xui-panel-manager

# اجرای دستی sync
sudo python3 /opt/xui-panel-manager/sync.py

# نمایش وضعیت
systemctl status xui-panel-manager-sync.timer

# نمایش لاگ‌ها
tail -f /var/log/xui-panel-manager.log

# ری‌استارت پنل وب
systemctl restart xui-panel-manager-web
```

### منوی Terminal:

```
╔═══════════════════════════════════════╗
║   XUI Panel Manager                  ║
╚═══════════════════════════════════════╝

1) Show system status
2) List servers
3) Add server
4) Remove server
5) Change sync interval
6) Enable/Disable auto timer
7) Run sync now
8) Manage packages
9) Web panel settings
10) Telegram bot settings
11) Show logs
12) Exit
```

---

## 📊 API Documentation

پنل وب یک REST API کامل دارد:

### Authentication
همه endpoint ها نیاز به session authentication دارند.

### Endpoints

#### 📌 Clients

**دریافت لیست کلاینت‌ها:**
```http
GET /api/clients
```

**به‌روزرسانی کلاینت:**
```http
POST /api/clients/{email}/update
Content-Type: application/json

{
  "days": 30,
  "traffic_gb": 100,
  "action": "set"  // "set", "add", or "reset"
}
```

**اعمال پکیج:**
```http
POST /api/clients/{email}/apply-package
Content-Type: application/json

{
  "package_id": "basic_30"
}
```

**Reset ترافیک:**
```http
POST /api/clients/{email}/reset
```

#### 📦 Packages

**لیست پکیج‌ها:**
```http
GET /api/packages
```

**اضافه کردن پکیج:**
```http
POST /api/packages
Content-Type: application/json

{
  "name": "Basic Package",
  "days": 30,
  "traffic_gb": 50,
  "price": "10"
}
```

**حذف پکیج:**
```http
DELETE /api/packages/{package_id}
```

#### 🖥️ Servers

**لیست سرورها:**
```http
GET /api/servers
```

**اضافه کردن سرور:**
```http
POST /api/servers
Content-Type: application/json

{
  "name": "Server-DE",
  "host": "192.168.1.100",
  "port": 22,
  "user": "root",
  "db_path": "/etc/x-ui/x-ui.db",
  "service_name": "x-ui"
}
```

**حذف سرور:**
```http
DELETE /api/servers/{index}
```

#### 🔄 Sync

**اجرای دستی sync:**
```http
POST /api/sync/trigger
```

#### 📊 Stats

**دریافت آمار:**
```http
GET /api/stats
```

Response:
```json
{
  "total_clients": 150,
  "active_clients": 120,
  "expired_clients": 10,
  "expiring_soon": 15,
  "total_servers": 3,
  "total_packages": 4,
  "total_used_gb": 5432.5,
  "total_quota_gb": 15000.0
}
```

---

## ⚙️ پیکربندی پیشرفته

### فایل‌های Config

```
/etc/xui-panel-manager/
├── config.json         # تنظیمات اصلی
├── servers.json        # لیست سرورها
├── packages.json       # پکیج‌های تعریف شده
├── users.json          # کاربران پنل وب
├── language.conf       # زبان پیش‌فرض
└── id_ed25519          # کلید SSH
```

### config.json

```json
{
  "local_db_path": "/etc/x-ui/x-ui.db",
  "local_service_name": "x-ui",
  "sync_interval_minutes": 60,
  "web_panel": {
    "enabled": true,
    "host": "0.0.0.0",
    "port": 8080,
    "secret_key": "..."
  },
  "telegram": {
    "enabled": true,
    "bot_token": "123456:ABC...",
    "chat_id": "123456789"
  },
  "backup": {
    "enabled": true,
    "keep_count": 10
  }
}
```

### تغییر پورت پنل وب:

```bash
sudo nano /etc/xui-panel-manager/config.json
# تغییر "port": 8080 به پورت دلخواه

sudo systemctl restart xui-panel-manager-web
```

---

## 🔍 عیب‌یابی

### مشکل SSH Connection

```bash
# تست دستی SSH
ssh -i /etc/xui-panel-manager/id_ed25519 root@SERVER_IP

# بررسی authorized_keys روی سرور مقصد
cat ~/.ssh/authorized_keys
```

### پنل وب باز نمی‌شود

```bash
# بررسی وضعیت
systemctl status xui-panel-manager-web

# مشاهده لاگ
journalctl -u xui-panel-manager-web -n 50

# ری‌استارت
systemctl restart xui-panel-manager-web
```

### Sync کار نمی‌کند

```bash
# اجرای دستی برای دیدن خطا
sudo python3 /opt/xui-panel-manager/sync.py

# بررسی لاگ
tail -f /var/log/xui-panel-manager.log

# بررسی تایمر
systemctl status xui-panel-manager-sync.timer
```

### Database Locked

سیستم به صورت خودکار:
1. چندین بار retry می‌کند
2. در صورت نیاز سرویس را موقتاً متوقف می‌کند
3. Backup قبل از تغییر ایجاد می‌کند

اگر باز هم مشکل دارید:
```bash
# توقف دستی سرویس
systemctl stop x-ui

# اجرای sync
python3 /opt/xui-panel-manager/sync.py

# شروع سرویس
systemctl start x-ui
```

---

## 💡 سناریوهای کاربردی

### 1️⃣ Multi-Location Setup
```
Germany Panel ────┐
USA Panel     ────┼──► Sync ──► همه یکسان
Singapore Panel ──┘
```
کاربر می‌تواند با یک اکانت از همه لوکیشن‌ها استفاده کند.

### 2️⃣ فروش پکیج‌های آماده
```
1. مشتری پکیج می‌خرد
2. شما در پنل client را پیدا می‌کنید
3. پکیج مناسب را Apply می‌کنید
4. به صورت خودکار در همه سرورها اعمال می‌شود
```

### 3️⃣ مدیریت متمرکز
```
تمام اطلاعات: یک پنل
ویرایش: یک جا
نتیجه: همه‌جا
```

---

##  🚀 Performance Tips

### بهینه‌سازی Sync

```json
{
  "sync_interval_minutes": 30  // برای sync سریع‌تر
}
```

### کاهش بار سرورها

```bash
# اولویت پایین به sync
nice -n 19 python3 /opt/xui-panel-manager/sync.py
```

---

## 🔐 امنیت

### توصیه‌های امنیتی:

1. **تغییر رمز پیش‌فرض**  
   حتماً رمز admin را تغییر دهید

2. **فایروال**
   ```bash
   ufw allow 8080/tcp  # فقط برای IP های مطمئن
   ```

3. **SSH Key**  
   کلید خصوصی را محافظت کنید:
   ```bash
   chmod 600 /etc/xui-panel-manager/id_ed25519
   ```

4. **HTTPS**  
   برای production از reverse proxy استفاده کنید:
   ```nginx
   server {
       listen 443 ssl;
       server_name panel.example.com;
       
       location / {
           proxy_pass http://localhost:8080;
       }
   }
   ```

---

## 🗑️ حذف کامل

```bash
# 1. توقف سرویس‌ها
sudo systemctl stop xui-panel-manager-web
sudo systemctl stop xui-panel-manager-sync.timer
sudo systemctl disable xui-panel-manager-web
sudo systemctl disable xui-panel-manager-sync.timer

# 2. حذف فایل‌ها
sudo rm -rf /opt/xui-panel-manager
sudo rm -rf /var/lib/xui-panel-manager
sudo rm /usr/local/bin/xui-panel-manager
sudo rm /etc/systemd/system/xui-panel-manager-*
sudo systemctl daemon-reload

# 3. حذف config (اختیاری)
sudo rm -rf /etc/xui-panel-manager

# 4. حذف لاگ (اختیاری)
sudo rm /var/log/xui-panel-manager.log
```

---

## 📞 پشتیبانی

### لاگ‌ها:
```bash
# لاگ اپلیکیشن
tail -f /var/log/xui-panel-manager.log

# لاگ systemd
journalctl -u xui-panel-manager-sync -f
journalctl -u xui-panel-manager-web -f
```

### اطلاعات سیستم:
```bash
# ورژن
cat /opt/xui-panel-manager/VERSION

# وضعیت کامل
sudo xui-panel-manager
# گزینه 1: Show system status
```

---

## 📝 Changelog

### v1.0.0 (Current)
- ✅ پنل مدیریت وب کامل
- ✅ سیستم پکیج‌های آماده
- ✅ چند زبانه (فارسی/انگلیسی)
- ✅ اعلان‌های تلگرام
- ✅ پشتیبان‌گیری خودکار
- ✅ Health monitoring
- ✅ REST API
- ✅ Dashboard با آمار

---

## 📄 License

MIT License - استفاده، تغییر و توزیع آزاد

---

## 🙏 تشکر

ساخته شده با ❤️ برای جامعه X-UI

**ستاره بدهید ⭐ اگر مفید بود!**

---

**توسعه‌دهندگان**: XUI Panel Manager Team  
**نسخه**: 1.0.0  
**تاریخ**: 2026
